# Super Hermes 使用说明

这是一个本地运行的 Hermes + OpenClaw 融合面板。Hermes 负责接任务和拆任务，执行器负责真正执行，Cockpit 负责可视化查看状态。

## 一键安装

### Linux / macOS

```bash
curl -fsSL https://github.com/YCL001/super-hermes/raw/main/install.sh | bash
```

### Windows

Windows 不要使用 `curl ... | cmd`。正确方式是先下载再执行：

```powershell
curl.exe -L "https://github.com/YCL001/super-hermes/raw/main/install.bat" -o install.bat
cmd /c install.bat
```

如果你已经克隆了仓库，也可以直接双击：

- `install.bat`

## 安装后会做什么

1. 检查 `Node.js`
2. 检查 `Git`
3. 检查 `Hermes CLI`
4. 执行 `npm install`
5. 自动创建本地配置目录和模板文件

已存在的本地配置不会被覆盖。

## 启动方式

### 只启动面板

```bash
npm run cockpit
```

浏览器访问：

`http://127.0.0.1:24318/`

### Windows 一键启动

直接双击：

- `一键启动.cmd`

它会自动：

1. 启动前端面板服务
2. 打开浏览器页面
3. 启动 Hermes CLI

## 需要你补充的配置

安装完成后，请检查并填写这些本地文件：

- `data/hermes-home/.env`
- `data/hermes-home/config.yaml`
- `data/openclaw-home/openclaw.json`
- `config/local-dev.json`

主要需要填写：

- API base URL
- API Key
- 模型 ID
- 默认 provider

## 依赖要求

- `Node.js` 18+
- `Git`
- `Hermes CLI`
- npm 依赖 `ws`
